services:
  - type: web
    name: zyvo-creator-3d
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "24"
      - key: PLATFORM_URL
        sync: false