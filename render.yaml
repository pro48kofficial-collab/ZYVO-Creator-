services:
  - type: web
    name: zyvo-creator
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ZYVO_PLATFORM_URL
        sync: false
      - key: CREATOR_SECRET
        sync: false
      - key: PORT
        value: "10000"
